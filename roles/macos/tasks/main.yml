---
- name: Install curl, ipcalc, pam-reattach, GNU sed and GNU watch
  community.general.homebrew:
    name:
      - "curl"
      - "ipcalc"
      - "pam-reattach"
      - "gnu-sed"
      - "watch"
    state: present

- name: Install taproom
  community.general.homebrew:
    name: "gromgit/brewtils/taproom"
    state: present

- name: Ensure /etc/pam.d/sudo_local exists
  ansible.builtin.copy:
    src: "{{ role_path }}/files/sudo_local"
    dest: "/etc/pam.d/sudo_local"
    remote_src: true
    owner: root
    group: wheel
    mode: "0644"
  become: true
